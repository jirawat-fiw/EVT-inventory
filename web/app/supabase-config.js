/* ============================================================
   EVT — ตั้งค่าเชื่อมต่อ Supabase
   เปลี่ยน 2 ค่านี้ให้เป็นของโปรเจกต์คุณ
   หาได้ที่: Supabase > Project Settings > API
   ============================================================ */
window.SUPABASE_URL      = "https://YOUR-PROJECT-ref.supabase.co"; // <- Project URL
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";                 // <- anon public key
